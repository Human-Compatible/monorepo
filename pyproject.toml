[tool.poetry]
name = "humancompatible"
version = "0.1.0"
description = ""
authors = []
license = "Apache-2.0"
readme = "README.md"

[[tool.poetry.packages]]
include = "humancompatible"
from = "libs"

[tool.poetry.dependencies]
python = ">=3.8"

[tool.poetry.dependencies.assistance]
path = "libs/assistance"
develop = true

[build-system]
requires = ["poetry-core==1.1.0"]
build-backend = "poetry.core.masonry.api"
